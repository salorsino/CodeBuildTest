version: 0.2
phases:
  build:
    commands:
      - echo "Zipping up function"
      - cd mobileapp_salTest && zip -r ../archive.zip ./*
      - cd ../mobileapp_salTest && zip -r ../archive.zip ./*
  post_build:
    commands:
      - cd ..
      - echo "Updating Lambda"
      - aws lambda update-function-code --function-name mobileapp_salTest --zip-file fileb://archive.zip
      - aws lambda update-function-code --function-name mobileapp_getDashboardInfo --zip-file fileb://archive.zip
      - echo "DONE!"